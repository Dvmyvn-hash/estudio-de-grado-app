# Dependencias de producción para GRADOMANÍA
# Compatible con despliegue en Render.com (Python 3.10+)

# Extracción y lectura de fuentes jurídicas (PDF y Word)
pypdf>=4.0.0
python-docx>=1.0.0

# SDK de autenticación Google (opcional, fallback nativo vía urllib/tokeninfo ya incluido)
google-auth>=2.20.0
